[package]
name = "tests"
version = "0.1.0"
edition = "2024"

[lib]
name = "tests"
path = "src/lib.rs"

[dependencies]
common = { path = "../common" }
approx = "0.5.1"
nalgebra = "0.35.0"
petgraph = { version = "0.8.1", default-features = false, features = [
    "stable_graph",
] }
common-macros = { path = "../common-macros" }